require 'config';

--[[

  NOTE: I don't guarantee that installing my config will work,
        if any errors persist then that's up to you.

  I. Entry:

     Hi, after you have cloned my nvim config,
     place it into your config path.

  II. Plugins Install:

  1. There are a total of 15 plugins that need
     to be downloaded, do the command below.

     :luafile ~/.config/nvim/lua/git.lua

  2. To make sure the configuration works  properly,
     we will need to download the files required
     for my japanese input plugin.

     ?: (Find this on any search engine)

     SKK-JISYO.geo
     SKK-JISYO.honifuwa
     SKK-JISYO.L

     Place the files in this path:
     ~/.config/nvim/skk/

  3. done.

--]]
