#!/bin/sh

export PS1="- "
xset r rate 200 50

export PATH=$PATH:/test
export LD_LIBRARY_PATH=/usr/lib:/usr/lib64:/usr/lib32
export XDG_CONFIG_HOME=$HOME/.config

export TERM=xterm
export VISUAL=vim
export EDITOR=vim

##
alias ls='ls --color=auto'
alias vim="vim -u \$HOME/.config/vim/vimrc"
alias firefox="firefox --enable-alsa"